services:
  client:
    build: .
    ports:
      - "8080:80"
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--spider", "http://127.0.0.1:80/"]
      interval: 15s
      timeout: 3s
      retries: 5
